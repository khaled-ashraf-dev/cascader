{
  "name": "cascade",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron --disable-gpu-sandbox .",
    "build": "electron-packager . Cascade --platform=win32 --arch=x64 --out dist --overwrite --icon=icon.ico --copy='config.json' --copy='python_scripts/**/*'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^24.8.5"
  }
}
